/* ---------------- */
/* Condition        */
/* ---------------- */

// if (true) {
//   console.log('이 조건은 항상 실행됨');
// } else {
//   console.log('조건에 부합하지 않음');
// }

/* let jsOfficial = 'ECMAScript';
let result = prompt('자바스크립트의 공식 이름은 무엇일까요?', '').toLowerCase();
if (result === jsOfficial.toLowerCase()) {
  console.log('정답입니다.');
} else {
  console.log('모르셨나요? 정답은 ECMAScript입니다!');
} */

/* let result = +prompt('숫자 하나를 입력해주세요', 0);

if (result > 0) {
  console.log(1);
} else if (result < 0) {
  console.log(-1);
} else {
  console.log(0);
} */

let a, b;
let result = a + b < 4 ? '미만' : '이상';

// 그 영화 봤니?
//     ↓
// Yes | No
//     | 영화 볼거니?
//           ↓
//       Yes | No

// 영화 봤니?
// let didWatchMovie = confirm('너 엘리멘탈 영화 봤니?', '');

// 영화 볼거니?

// if 문(statement)
/* if (!didWatchMovie) {
  let goingToWatchMovie = confirm('너 영화 볼거니?');
  if (goingToWatchMovie) {
    let withWho = confirm('혼자 볼거니?');
    if (withWho) {
      console.log('나랑 보러 갈래?');
    } else {
      console.log('재밌게 보렴');
    }
  }
} else {
  let alone = confirm('너 혼자 봤니?');
  if (alone) {
    let didCry = confirm('너 울었니?');
    if (didCry) {
      console.log('너 ... 찌질했네...');
    } else {
      console.log('너 T야?');
    }
  }
} */

// else 절(caluse)

// else if 복수 조건 처리

// 조건부 연산자

// 멀티 조건부 연산자 식
